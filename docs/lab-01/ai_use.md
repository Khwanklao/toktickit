# Lab 1 — AI Use and Reflection  (fill this in)

**LLM/agent used:** <name>

## Selected key prompts (6–10)
| # | Prompt (summarised) | What I did with the result |
|---|---------------------|----------------------------|
| 1 | Please set up the TokTickIT project foundation for Issue 1 on the current branch (feature/1-project-foundation) according to these rules:

1.Directory Structure: Create root folders client, server, prisma, docs/lab-01, and tests/lab-01.

2.Frontend (client/): Set up React + TypeScript + Vite with Bootstrap installed and imported in main.tsx.

3.Backend (server/): Set up Node.js + Express + TypeScript backend with tsconfig.json and basic src/index.ts.

4.Database (prisma/): Initialize Prisma with PostgreSQL provider and create .env.example.

5.Testing & Config: Configure Vitest and Supertest scripts in package.json, add .gitignore (ignoring node_modules and .env), and create a basic README.md.
Do not write functional feature logic beyond this setup scope

Reviewed the generated directory structure and accepted the changes as it matched the lab requirements
| 2 |  |  |

## Reflection
Two or three sentences: what made your prompts better, and one place you had to
correct or reject what the agent produced.