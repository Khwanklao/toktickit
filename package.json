{
  "name": "toktickit",
  "version": "1.0.0",
  "private": true,
  "description": "TokTickIT - IT Helpdesk Support & Ticketing System",
  "scripts": {
    "test": "npm --prefix client test && npm --prefix server test",
    "test:client": "npm --prefix client test",
    "test:server": "npm --prefix server test",
    "dev:client": "npm --prefix client dev",
    "dev:server": "npm --prefix server dev",
    "build:client": "npm --prefix client build",
    "build:server": "npm --prefix server build"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^7.10.0"
  },
  "devDependencies": {
    "prisma": "^8.0.0-rc.11"
  }
}
